---
database:
    host: localhost
    port: 5432
    user: jianingy
    schema: almartest
    table: object
    min_connections: 1
    max_connections: 4

server:
    port: 9999
    max_threads: 4
    role:
        reader: True
        writer: True

proxy:
    port: 55555
    max_threads: 4

model:
    - ~/devel/almar/test/cfg/model.yaml

# vim: ts=4 sw=4 ai et
