---
database:
    host: localhost
    port: 5432
    user: jianingy
    schema: almartest
    table: object
    min_connections: 4
    max_connections: 8

server:
    port: 9999
    max_threads: 4

model:
    - ~/devel/almar/test/cfg/model.yaml

# vim: ts=4 sw=4 ai et
